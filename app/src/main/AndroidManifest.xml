<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.anexception.shamse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Iranexceptionshamse"
        android:name="ir.anexception.MyApplication">
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity" />

        <service
            android:name=".tiles.ShamseTile"
            android:enabled="true"
            android:exported="true"
            android:icon="@drawable/shamse"
            android:label="@string/tile_shamse"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        <service
            android:name=".tiles.YaMahdiTile"
            android:enabled="true"
            android:exported="true"
            android:icon="@drawable/ya_mahdi"
            android:label="@string/tile_ya_mahdi"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        <service
            android:name=".tiles.YaFatemeTile"
            android:enabled="true"
            android:exported="true"
            android:icon="@drawable/ya_fateme"
            android:label="@string/tile_ya_fateme"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        <service
            android:name=".tiles.HajGhasemTile"
            android:enabled="true"
            android:exported="true"
            android:icon="@drawable/haj_ghasem"
            android:label="@string/tile_haj_ghasem"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>

</manifest>